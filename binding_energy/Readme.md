extend_quad.f is the Fortran code to compute the binding energy. Ps.data is a sample input file.

The input file is structured as follows:
nonlinear variational parameter values
omega value
print option on/off (1/0)
